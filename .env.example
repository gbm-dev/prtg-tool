# PRTG CLI Tool - Environment Configuration
# Copy this file to .env and fill in your values

# PRTG Server URL
PRTG_URL=https://prtg.example.com

# API Token (get from: Setup > My Account > API Keys)
# Use either PRTG_API_TOKEN or separate RO/RW tokens

# Option 1: Single API token (for all operations)
PRTG_API_TOKEN=YOUR_API_TOKEN_HERE

# Option 2: Separate read-only and read-write tokens
# The tool will prefer RW token if available, fall back to RO
# PRTG_API_TOKEN_RO=YOUR_READ_ONLY_TOKEN_HERE
# PRTG_API_TOKEN_RW=YOUR_READ_WRITE_TOKEN_HERE

# Optional: Profile to use from config file
# PRTG_PROFILE=production

# Optional: Config file path
# PRTG_CONFIG=/path/to/config

# Optional: Disable SSL verification (not recommended)
# PRTG_NO_VERIFY_SSL=1

# Optional: Output format
# PRTG_OUTPUT_FORMAT=json

# Optional: Enable debug mode
# PRTG_DEBUG=1
